{% extends 'base.html.twig' %}

{% block body %}
<h2>Create Employee</h2>

<div class="card p-4 shadow-sm">
    {{ include('employee/_form.html.twig') }}
</div>

<a href="{{ path('app_employee_index') }}" class="btn btn-secondary mt-3">Back</a>
{% endblock %}